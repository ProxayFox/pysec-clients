{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pysec-clients Documentation","text":"<p>This site contains structured documentation for packages in the <code>pysec-clients</code> workspace.</p>"},{"location":"#available-package-docs","title":"Available package docs","text":"<ul> <li>MDE Client: Microsoft Defender for Endpoint package documentation, including tutorials, how-to guides, reference, and explanation.</li> </ul>"},{"location":"#repository-entry-points","title":"Repository entry points","text":"<ul> <li>The workspace overview remains in the repository root README.</li> <li>Package-specific quick-start material remains in each package README.</li> <li>This site is the place for structured, task-oriented documentation.</li> </ul>"},{"location":"mde_client/","title":"MDE Client Documentation","text":"<p>This documentation covers the <code>mde-client</code> package from four angles:</p> <ul> <li>Tutorials help a new user reach a first successful outcome.</li> <li>How-to guides solve common package tasks.</li> <li>Reference pages describe the package surface as it exists today.</li> <li>Explanation pages clarify the design choices behind the client.</li> </ul> <p><code>mde-client</code> is a Python client for Microsoft Defender for Endpoint built around a single <code>MDEClient</code>, endpoint properties such as <code>client.machines</code> and <code>client.alerts</code>, and lazy result wrappers that materialize only when you call a terminal method such as <code>to_dicts()</code> or <code>to_polars()</code>.</p>"},{"location":"mde_client/#choose-a-starting-point","title":"Choose a starting point","text":"<ul> <li>New to the package: Get started</li> <li>Need to solve one task: How-to guides</li> <li>Need exact API details: Reference</li> <li>Need to understand the design: Explanation</li> </ul>"},{"location":"mde_client/#package-overview","title":"Package overview","text":"<p>The current package surface is organized around these ideas:</p> <ul> <li><code>MDEClient</code> manages authentication and the underlying HTTP session.</li> <li>Each endpoint family is exposed as a property on the client.</li> <li>Most endpoint methods return a lazy results wrapper instead of fetching immediately.</li> <li>The same result wrapper pattern is used across JSON-backed and export-backed endpoints.</li> <li><code>httpx.Client</code> and <code>msal.TokenCache</code> can be injected for testing and customization.</li> </ul>"},{"location":"mde_client/#documentation-map","title":"Documentation map","text":""},{"location":"mde_client/#tutorials","title":"Tutorials","text":"<ul> <li>Get started: install the package, authenticate, and fetch your first machine records.</li> </ul>"},{"location":"mde_client/#how-to-guides","title":"How-to guides","text":"<ul> <li>How-to index</li> <li>Authenticate with client credentials</li> <li>List and filter machines</li> <li>Get related data for one machine</li> <li>Run an advanced hunting query</li> <li>Work with result formats</li> <li>Use export-backed endpoints</li> <li>Inject a custom HTTP client or token cache</li> </ul>"},{"location":"mde_client/#reference","title":"Reference","text":"<ul> <li>Reference index</li> <li>MDEClient</li> <li>Authentication</li> <li>Results wrappers</li> <li>Query models</li> <li>ViaFiles</li> <li>Endpoint reference</li> </ul>"},{"location":"mde_client/#explanation","title":"Explanation","text":"<ul> <li>Explanation index</li> <li>Lazy results and caching</li> <li>Why query models keep Defender field names</li> <li>How export-backed endpoints work</li> <li>Why the client supports dependency injection</li> </ul>"},{"location":"mde_client/#scope-of-this-docs-set","title":"Scope of this docs set","text":"<p>This docs tree focuses on the <code>mde-client</code> package under <code>src/mde-client/</code>. It does not try to cover Azure portal setup step by step, and it does not replace the repository-level workflow notes in the root README.</p>"},{"location":"mde_client/explanation/","title":"Explanation","text":"<p>Explanation pages are for understanding, not task execution.</p> <ul> <li>Lazy results and caching</li> <li>Why query models keep Defender field names</li> <li>How export-backed endpoints work</li> <li>Why the client supports dependency injection</li> </ul> <p>If you want step-by-step instructions, use How-to guides or the Get started tutorial.</p>"},{"location":"mde_client/explanation/dependency-injection/","title":"Why the Client Supports Dependency Injection","text":"<p><code>MDEClient</code> accepts both an <code>httpx.Client</code> and an <code>msal.TokenCache</code> because transport and authentication state are common sources of operational variation.</p>"},{"location":"mde_client/explanation/dependency-injection/#http-client-injection","title":"HTTP client injection","text":"<p>Allowing a caller to pass an <code>httpx.Client</code> makes it possible to:</p> <ul> <li>use a mock transport in tests</li> <li>set custom timeouts</li> <li>configure proxies or custom networking behavior</li> <li>share a client configuration that already exists in the host application</li> </ul>"},{"location":"mde_client/explanation/dependency-injection/#token-cache-injection","title":"Token cache injection","text":"<p>Allowing a caller to pass an MSAL token cache makes it possible to:</p> <ul> <li>persist tokens between runs</li> <li>reuse an application-specific cache strategy</li> <li>avoid coupling package behavior to one fixed cache implementation</li> </ul>"},{"location":"mde_client/explanation/dependency-injection/#why-this-is-a-good-fit-for-this-package","title":"Why this is a good fit for this package","text":"<p>The package is a client library, not an application. It should make the common path easy while still leaving room for callers with stronger requirements around tests, networking, or auth persistence.</p> <p>That is why the constructor owns sensible defaults but still exposes both seams.</p>"},{"location":"mde_client/explanation/export-backed-endpoints/","title":"How Export-Backed Endpoints Work","text":"<p>Some Defender APIs do not return the final dataset directly. Instead, the initial response contains <code>exportFiles</code> URLs that point to blob storage.</p>"},{"location":"mde_client/explanation/export-backed-endpoints/#what-the-client-does-with-those-urls","title":"What the client does with those URLs","text":"<p>For export-backed endpoint wrappers, the package:</p> <ul> <li>calls the initial endpoint</li> <li>extracts the export URLs</li> <li>downloads the blobs concurrently</li> <li>decompresses them when needed</li> <li>parses newline-delimited JSON records</li> <li>appends the records into the same Arrow-backed container used elsewhere in the client</li> </ul>"},{"location":"mde_client/explanation/export-backed-endpoints/#why-the-user-facing-api-stays-the-same","title":"Why the user-facing API stays the same","text":"<p>The package hides the extra transport layer so callers can keep using the same materialization methods they already know:</p> <pre><code>rows = client.device_av_health.get_all_files().to_dicts()\n</code></pre> <p>That makes export-backed endpoints feel like ordinary endpoint calls even though the underlying fetch path is more complex.</p>"},{"location":"mde_client/explanation/export-backed-endpoints/#why-this-matters","title":"Why this matters","text":"<p>Without this abstraction, every caller would need to reimplement blob download, gzip handling, newline parsing, and retry behavior.</p> <p>The current design keeps that logic in one place and reuses it across endpoint families.</p>"},{"location":"mde_client/explanation/lazy-results/","title":"Lazy Results and Caching","text":"<p>The package is built around lazy result wrappers because Defender responses vary in size and cost.</p> <p>Instead of fetching immediately, an endpoint method returns a lightweight wrapper that remembers:</p> <ul> <li>which path to call</li> <li>which query parameters to send</li> <li>whether the endpoint is single-item, paginated, or file-backed</li> </ul> <p>The HTTP request only happens when you choose an output form such as <code>to_dicts()</code> or <code>to_polars()</code>.</p>"},{"location":"mde_client/explanation/lazy-results/#why-this-helps","title":"Why this helps","text":"<p>This design gives the caller one stable interface regardless of endpoint family.</p> <p>It also lets the package postpone work until the caller knows how they want to consume the data.</p>"},{"location":"mde_client/explanation/lazy-results/#why-there-is-a-cache","title":"Why there is a cache","text":"<p>Once materialized, the wrapper keeps the parsed data in memory. That avoids repeated network calls when you need multiple representations of the same result:</p> <pre><code>results = client.machines.get_all()\nrows = results.to_dicts()\nframe = results.to_polars()\n</code></pre> <p>Without caching, each terminal method would repeat the request.</p>"},{"location":"mde_client/explanation/lazy-results/#why-refresh-exists","title":"Why <code>refresh()</code> exists","text":"<p>Caching is useful, but some workflows need a fresh read. <code>refresh()</code> drops the cached container so the next terminal method triggers a new API call.</p>"},{"location":"mde_client/explanation/lazy-results/#tradeoff","title":"Tradeoff","text":"<p>The design favors a consistent and ergonomic consumer API over strict eagerness. In return, the caller needs to understand that constructing a results object is not the same as executing the request.</p>"},{"location":"mde_client/explanation/query-models/","title":"Why Query Models Keep Defender Field Names","text":"<p>The query models intentionally preserve upstream Defender field names such as <code>healthStatus</code> and <code>creationDateTimeUtc</code>.</p>"},{"location":"mde_client/explanation/query-models/#why-not-normalize-everything-to-snake_case","title":"Why not normalize everything to snake_case","text":"<p>Normalizing the query surface would create a second vocabulary on top of the upstream API. That tends to make documentation, debugging, and cross-checking harder.</p> <p>When a user reads the Defender API docs or inspects a payload, the field names in <code>mde-client</code> line up with what they already see.</p>"},{"location":"mde_client/explanation/query-models/#what-this-buys-the-user","title":"What this buys the user","text":"<ul> <li>fewer translation mistakes when building filters</li> <li>easier comparison with raw Defender payloads</li> <li>less hidden behavior in the query serialization layer</li> </ul>"},{"location":"mde_client/explanation/query-models/#tradeoff","title":"Tradeoff","text":"<p>The package accepts that some query models look less Pythonic than a fully normalized surface. The goal is operational clarity rather than stylistic purity.</p>"},{"location":"mde_client/how-to/","title":"How-to Guides","text":"<p>These guides are task-oriented. Each page solves one problem without trying to teach the entire package.</p> <ul> <li>Authenticate with client credentials</li> <li>List and filter machines</li> <li>Get related data for one machine</li> <li>Run an advanced hunting query</li> <li>Work with result formats</li> <li>Use export-backed endpoints</li> <li>Inject a custom HTTP client or token cache</li> </ul> <p>If you are new to the package, start with Get started instead of these guides.</p>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/","title":"Authenticate with Client Credentials","text":"<p>Use this guide when you already know you want app-only authentication and need the package-level steps.</p>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/#what-you-need","title":"What you need","text":"<ul> <li>tenant ID</li> <li>client ID</li> <li>client secret</li> <li>Defender application permissions granted to the app registration</li> </ul>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/#create-the-client","title":"Create the client","text":"<pre><code>from mde_client import MDEClient\n\nclient = MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n)\n</code></pre> <p><code>MDEClient</code> uses MSAL under the hood and requests the Defender default scope.</p>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/#reuse-the-client-safely","title":"Reuse the client safely","text":"<p>Prefer the context-manager form so the underlying <code>httpx.Client</code> is closed promptly:</p> <pre><code>from mde_client import MDEClient\n\nwith MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n) as client:\n    machines = client.machines.get_all().to_dicts()\n</code></pre> <p>If you do not use <code>with</code>, call <code>client.close()</code> yourself.</p>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/#persist-tokens-with-a-cache","title":"Persist tokens with a cache","text":"<p>If you want MSAL token caching outside the default in-memory behavior, inject a cache object:</p> <pre><code>import msal\n\nfrom mde_client import MDEClient\n\ncache = msal.SerializableTokenCache()\n\nclient = MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n    token_cache=cache,\n)\n</code></pre>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/#handle-authentication-failures","title":"Handle authentication failures","text":"<p>Authentication failures raise <code>AuthenticationError</code>:</p> <pre><code>from mde_client import AuthenticationError, MDEClient\n\ntry:\n    with MDEClient(\n        tenant_id=\"YOUR_TENANT_ID\",\n        client_id=\"YOUR_CLIENT_ID\",\n        client_secret=\"YOUR_CLIENT_SECRET\",\n    ) as client:\n        client.machines.get_all().to_dicts()\nexcept AuthenticationError as exc:\n    print(exc)\n</code></pre>"},{"location":"mde_client/how-to/authenticate-with-client-credentials/#next-references","title":"Next references","text":"<ul> <li>Authentication reference</li> <li>MDEClient reference</li> </ul>"},{"location":"mde_client/how-to/get-machine-related-data/","title":"Get Related Data for One Machine","text":"<p>Use this guide when you already have a Defender machine ID and need related datasets from the machine surface.</p>"},{"location":"mde_client/how-to/get-machine-related-data/#fetch-the-machine-itself","title":"Fetch the machine itself","text":"<pre><code>machine = client.machines.get(\"machine-guid\").to_dicts()[0]\n</code></pre>"},{"location":"mde_client/how-to/get-machine-related-data/#fetch-related-records","title":"Fetch related records","text":"<p>The machine endpoint groups several machine-scoped lookups under the same property:</p> <pre><code>machine_id = \"machine-guid\"\n\nalerts = client.machines.alerts(machine_id).to_dicts()\nlogon_users = client.machines.logonusers(machine_id).to_dicts()\nsoftware = client.machines.software(machine_id).to_dicts()\nvulnerabilities = client.machines.vulnerabilities(machine_id).to_dicts()\nrecommendations = client.machines.recommendations(machine_id).to_dicts()\n</code></pre> <p>Each call returns a lazy results wrapper. The materialization method is what performs the request.</p>"},{"location":"mde_client/how-to/get-machine-related-data/#handle-empty-related-collections-safely","title":"Handle empty related collections safely","text":"<p>Machine-related subresources are not guaranteed to contain rows for every machine. A practical pattern is to materialize the collection and branch on emptiness:</p> <pre><code>machine_id = \"machine-guid\"\n\nsoftware_rows = client.machines.software(machine_id).to_dicts()\nif software_rows:\n    print(software_rows[0][\"name\"])\n\nvulnerability_rows = client.machines.vulnerabilities(machine_id).to_dicts()\nif vulnerability_rows:\n    print(vulnerability_rows[0][\"name\"])\n</code></pre> <p>The same approach works for alerts, recommendations, missing KBs, and logon users.</p>"},{"location":"mde_client/how-to/get-machine-related-data/#get-missing-kbs","title":"Get missing KBs","text":"<p>If you need missing KB data for a machine:</p> <pre><code>missing_kbs = client.machines.getmissingkbs(\"machine-guid\").to_dicts()\n</code></pre> <p>If you are building an inspection workflow, it is reasonable to fetch the machine once and then branch into only the related collections you need:</p> <pre><code>machine_id = \"machine-guid\"\n\nmachine = client.machines.get(machine_id).to_dicts()[0]\nlogon_users = client.machines.logonusers(machine_id).to_dicts()\nmissing_kbs = client.machines.getmissingkbs(machine_id).to_dicts()\n</code></pre> <p>This keeps the flow explicit and avoids assuming that every related collection will have data.</p>"},{"location":"mde_client/how-to/get-machine-related-data/#choose-the-simplest-format-first","title":"Choose the simplest format first","text":"<p>Start with <code>to_dicts()</code> unless you already know you need Arrow or Polars. It keeps the integration simple and makes debugging easier.</p> <p>If you later need tabular processing, you can always re-materialize the same wrapper into Arrow or Polars without building a different request object.</p>"},{"location":"mde_client/how-to/get-machine-related-data/#next-references","title":"Next references","text":"<ul> <li>machines endpoint reference</li> <li>Work with result formats</li> </ul>"},{"location":"mde_client/how-to/inject-http-client-and-token-cache/","title":"Inject a Custom HTTP Client or Token Cache","text":"<p>Use this guide when you need custom HTTP behavior, a mock transport for tests, or persistent MSAL token caching.</p>"},{"location":"mde_client/how-to/inject-http-client-and-token-cache/#inject-an-httpxclient","title":"Inject an <code>httpx.Client</code>","text":"<pre><code>import httpx\n\nfrom mde_client import MDEClient\n\nhttp_client = httpx.Client(timeout=httpx.Timeout(60.0))\n\nclient = MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n    http_client=http_client,\n)\n</code></pre> <p>This is useful for custom transports, proxy configuration, or explicit timeouts.</p>"},{"location":"mde_client/how-to/inject-http-client-and-token-cache/#inject-a-token-cache","title":"Inject a token cache","text":"<pre><code>import msal\n\nfrom mde_client import MDEClient\n\ncache = msal.SerializableTokenCache()\n\nclient = MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n    token_cache=cache,\n)\n</code></pre>"},{"location":"mde_client/how-to/inject-http-client-and-token-cache/#combine-both-for-tests","title":"Combine both for tests","text":"<pre><code>import httpx\nimport msal\n\nfrom mde_client import MDEClient\n\nclient = MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n    http_client=httpx.Client(transport=mock_transport),\n    token_cache=msal.TokenCache(),\n)\n</code></pre>"},{"location":"mde_client/how-to/inject-http-client-and-token-cache/#close-injected-clients","title":"Close injected clients","text":"<p>If you inject your own <code>httpx.Client</code>, keep ownership in mind. Using <code>with MDEClient(...) as client:</code> still closes the underlying client when the context exits.</p>"},{"location":"mde_client/how-to/inject-http-client-and-token-cache/#next-references","title":"Next references","text":"<ul> <li>MDEClient reference</li> <li>Why the client supports dependency injection</li> </ul>"},{"location":"mde_client/how-to/list-and-filter-machines/","title":"List and Filter Machines","text":"<p>Use this guide when you need to query the machines inventory and control filtering or pagination.</p>"},{"location":"mde_client/how-to/list-and-filter-machines/#list-all-machines","title":"List all machines","text":"<pre><code>from mde_client import MDEClient\n\nwith MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n) as client:\n    machines = client.machines.get_all().to_dicts()\n</code></pre> <p>Without <code>$top</code> or <code>$skip</code>, the client paginates automatically until it has collected the full result set.</p> <p><code>MachinesQuery()</code> also includes a default <code>pageSize</code> value when you construct one without overrides, so the client can page through the collection in larger chunks.</p>"},{"location":"mde_client/how-to/list-and-filter-machines/#filter-by-defender-fields","title":"Filter by Defender fields","text":"<p>Use <code>MachinesQuery</code> and keep Defender field names as they appear in the API:</p> <pre><code>from mde_client import MDEClient\nfrom mde_client.endpoints.machines import MachinesQuery\n\nquery = MachinesQuery(\n    healthStatus=\"Active\",\n    exposureLevel=\"High\",\n    page_size=500,\n)\n\nwith MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n) as client:\n    machines = client.machines.get_all(query).to_dicts()\n</code></pre> <p>Common fields include <code>healthStatus</code>, <code>machineTags</code>, <code>osPlatform</code>, <code>riskScore</code>, and <code>rbacGroupId</code>.</p> <p>You can also combine filters or pass lists for <code>in (...)</code> queries:</p> <pre><code>from mde_client.endpoints.machines import MachinesQuery\n\nquery = MachinesQuery(\n    healthStatus=\"Active\",\n    osPlatform=\"Windows10\",\n    id=[\"machine-1\", \"machine-2\"],\n)\n\nrows = client.machines.get_all(query).to_dicts()\n</code></pre> <p>The query builder joins multiple fields with <code>and</code> and translates string lists into Defender-compatible <code>in (...)</code> filters.</p>"},{"location":"mde_client/how-to/list-and-filter-machines/#limit-the-result-set","title":"Limit the result set","text":"<p>Set <code>top</code> when you only want one page of results:</p> <pre><code>from mde_client.endpoints.machines import MachinesQuery\n\nquery = MachinesQuery(top=100)\n</code></pre> <p>When <code>top</code> or <code>skip</code> is set, the client does not auto-paginate.</p> <p>This is useful for discovery workflows where you want one bounded page and then plan to inspect the result in a dataframe:</p> <pre><code>from mde_client.endpoints.machines import MachinesQuery\n\nquery = MachinesQuery(top=512)\nframe = client.machines.get_all(query).to_polars()\n</code></pre>"},{"location":"mde_client/how-to/list-and-filter-machines/#continue-from-an-offset","title":"Continue from an offset","text":"<p>Use <code>skip</code> to request a later slice of the collection:</p> <pre><code>from mde_client.endpoints.machines import MachinesQuery\n\nquery = MachinesQuery(skip=1000, top=100)\n</code></pre> <p>This is useful when you want deterministic page-sized batches rather than automatic full collection retrieval.</p>"},{"location":"mde_client/how-to/list-and-filter-machines/#find-by-ip-or-tag","title":"Find by IP or tag","text":"<p><code>MachinesEndpoint</code> also exposes helper lookups for specific machine discovery workflows:</p> <pre><code>from datetime import datetime, timedelta, timezone\n\ntimestamp = datetime.now(timezone.utc) - timedelta(days=29)\nrecord = client.machines.findbyip(\"10.0.0.25\", timestamp).to_dicts()\ntagged = client.machines.tag(\"production\", useStartsWithFilter=True).to_dicts()\n</code></pre> <p>Use <code>useStartsWithFilter=True</code> when your tag input is a prefix rather than an exact tag.</p> <p>If you already have a page of machine records in Polars, a practical pattern is to extract candidate IP addresses first and then call <code>findbyip()</code> on one of them:</p> <pre><code>import polars as pl\n\nfrom datetime import datetime, timedelta, timezone\nfrom mde_client.endpoints.machines import MachinesQuery\n\nframe = client.machines.get_all(MachinesQuery(top=512)).to_polars()\nip_list = (\n    frame.select(pl.col(\"ipAddresses\"))\n    .explode(\"ipAddresses\")\n    .select(pl.col(\"ipAddresses\").struct.field(\"ipAddress\").alias(\"ipAddress\"))\n    .drop_nulls()\n    .get_column(\"ipAddress\")\n    .cast(pl.String)\n    .unique()\n    .to_list()\n)\n\nif ip_list:\n    timestamp = datetime.now(timezone.utc) - timedelta(days=29)\n    matches = client.machines.findbyip(ip_list[0], timestamp).to_dicts()\n</code></pre>"},{"location":"mde_client/how-to/list-and-filter-machines/#next-references","title":"Next references","text":"<ul> <li>machines endpoint reference</li> <li>Query models reference</li> </ul>"},{"location":"mde_client/how-to/run-advanced-hunting/","title":"Run an Advanced Hunting Query","text":"<p>Use this guide when you want to execute a Defender advanced hunting query through the package.</p>"},{"location":"mde_client/how-to/run-advanced-hunting/#run-the-query","title":"Run the query","text":"<pre><code>from mde_client import MDEClient\n\nquery = \"\"\"\nDeviceInfo\n| take 10\n\"\"\"\n\nwith MDEClient(\n    tenant_id=\"YOUR_TENANT_ID\",\n    client_id=\"YOUR_CLIENT_ID\",\n    client_secret=\"YOUR_CLIENT_SECRET\",\n) as client:\n    rows = client.advanced_queries.run(query).to_dicts()\n</code></pre> <p>The <code>run()</code> method returns its own results wrapper with the same terminal methods used by other endpoints.</p>"},{"location":"mde_client/how-to/run-advanced-hunting/#convert-to-a-dataframe","title":"Convert to a dataframe","text":"<pre><code>frame = client.advanced_queries.run(query).to_polars()\n</code></pre> <p>This is useful when you plan to reshape or aggregate the output locally.</p>"},{"location":"mde_client/how-to/run-advanced-hunting/#refresh-the-query","title":"Refresh the query","text":"<p>If you want to rerun the same query after the results have already been materialized:</p> <pre><code>results = client.advanced_queries.run(query)\nfirst = results.to_dicts()\nfresh = results.refresh().to_dicts()\n</code></pre>"},{"location":"mde_client/how-to/run-advanced-hunting/#notes","title":"Notes","text":"<ul> <li>The advanced hunting result schema is not fixed in the same way as the endpoint schemas backed by generated Arrow contracts.</li> <li>The wrapper still exposes the same <code>to_dicts()</code>, <code>to_json()</code>, <code>to_arrow()</code>, <code>to_polars()</code>, and <code>refresh()</code> interface.</li> </ul>"},{"location":"mde_client/how-to/run-advanced-hunting/#next-references","title":"Next references","text":"<ul> <li>advanced_queries endpoint reference</li> <li>Results wrappers reference</li> </ul>"},{"location":"mde_client/how-to/use-export-backed-endpoints/","title":"Use Export-Backed Endpoints","text":"<p>Use this guide when the Defender endpoint you need returns export URLs instead of embedding the final dataset directly in the initial response.</p>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#call-the-endpoint-specific-method","title":"Call the endpoint-specific method","text":"<p>The package already wraps several export-backed workflows for you:</p> <pre><code>browser_extensions = client.browser_extension.get_all_files().to_dicts()\ncertificates = client.certificate_inventory.get_all_files().to_dicts()\ndevice_av = client.device_av_health.get_all_files().to_dicts()\nbaselines = client.baseline_configurations.get_all_files().to_dicts()\n</code></pre> <p>The result wrapper handles the download step internally. You still use the same terminal methods as any other endpoint.</p> <p>The export downloader handles both plain NDJSON blobs and gzipped NDJSON blobs, so you do not need to special-case those formats in normal endpoint usage.</p>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#prefer-the-endpoint-wrapper-over-raw-export-urls","title":"Prefer the endpoint wrapper over raw export URLs","text":"<p>Most callers do not need to use <code>ViaFiles</code> directly. The endpoint wrapper already knows:</p> <ul> <li>how to detect the export URLs in the response</li> <li>how to stream and parse newline-delimited JSON</li> <li>how to coerce records into the endpoint schema</li> </ul>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#use-the-json-backed-form-when-available","title":"Use the JSON-backed form when available","text":"<p>Some surfaces offer both a regular JSON endpoint and a via-files form:</p> <pre><code>json_rows = client.browser_extension.get_all().to_dicts()\nfile_rows = client.browser_extension.get_all_files().to_dicts()\n</code></pre> <p>Choose the form that matches the size and workflow you need.</p>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#use-viafiles-directly-when-you-already-have-export-urls","title":"Use <code>ViaFiles</code> directly when you already have export URLs","text":"<p>Most callers should stay with the endpoint wrappers, but direct <code>ViaFiles</code> usage is available when you already have export URLs and want to stream them into your own Arrow container:</p> <pre><code>import asyncio\n\nfrom http_to_arrow import ArrowRecordContainer\n\nfrom mde_client import ViaFiles, ViaFilesConfig\n\nurls = [\n    \"https://blob.example.com/a.json\",\n    \"https://blob.example.com/b.json\",\n]\n\ncontainer = ArrowRecordContainer(schema=None)\ndownloader = ViaFiles(ViaFilesConfig(download_workers=2, client_timeout=30))\n\ncontainer = asyncio.run(downloader.download_export_files(urls, container))\ntable = container.to_table()\n</code></pre> <p>This path is useful when you want to tune worker count, timeout, retry settings, or parse batch size directly.</p>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#apply-a-record-transform-during-download","title":"Apply a record transform during download","text":"<p>If each NDJSON record wraps the payload you actually want, pass <code>record_transform</code> to normalize it during download:</p> <pre><code>import asyncio\n\nfrom http_to_arrow import ArrowRecordContainer\n\nfrom mde_client import ViaFiles\n\ndef unwrap(record: dict[str, object]) -&gt; dict[str, object]:\n    inner = dict(record.get(\"wrapper\", {}))\n    for key, value in record.items():\n        if key != \"wrapper\":\n            inner[key] = value\n    return inner\n\ncontainer = ArrowRecordContainer(schema=None)\ncontainer = asyncio.run(\n    ViaFiles().download_export_files(urls, container, record_transform=unwrap)\n)\n</code></pre> <p>This is the same mechanism the package uses internally for export-backed endpoints whose blob records need reshaping.</p>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#know-the-failure-model","title":"Know the failure model","text":"<p>Export downloads retry transient failures. Confirmed-empty blobs are skipped after retry logging. A repeated hard failure raises a runtime error.</p> <p>Practical behaviors to expect:</p> <ul> <li>multiple export URLs are merged into the same container</li> <li>an empty URL list returns immediately without error</li> <li>SAS query strings are redacted from logs</li> <li>confirmed-empty blobs are skipped rather than treated as fatal failures</li> </ul>"},{"location":"mde_client/how-to/use-export-backed-endpoints/#next-references","title":"Next references","text":"<ul> <li>ViaFiles reference</li> <li>How export-backed endpoints work</li> </ul>"},{"location":"mde_client/how-to/work-with-result-formats/","title":"Work with Result Formats","text":"<p>Use this guide when you need the same endpoint data in different in-memory representations.</p>"},{"location":"mde_client/how-to/work-with-result-formats/#start-from-a-results-wrapper","title":"Start from a results wrapper","text":"<pre><code>results = client.machines.get_all()\n</code></pre> <p>At this stage the request has not run yet.</p> <p>This lazy behavior is intentional. Constructing the wrapper is cheap, and the first terminal method decides when the request actually happens.</p>"},{"location":"mde_client/how-to/work-with-result-formats/#convert-to-python-dictionaries","title":"Convert to Python dictionaries","text":"<pre><code>rows = results.to_dicts()\n</code></pre> <p>Use this when you want the simplest Python-native representation.</p>"},{"location":"mde_client/how-to/work-with-result-formats/#convert-to-json","title":"Convert to JSON","text":"<pre><code>payload = results.to_json()\npretty = results.to_json(indent=True)\n</code></pre> <p><code>to_json(indent=True)</code> returns a formatted JSON string.</p>"},{"location":"mde_client/how-to/work-with-result-formats/#convert-to-arrow-or-polars","title":"Convert to Arrow or Polars","text":"<pre><code>table = results.to_arrow()\nframe = results.to_polars()\n</code></pre> <p>Use these when you need columnar or dataframe-style processing.</p>"},{"location":"mde_client/how-to/work-with-result-formats/#reuse-the-same-fetched-payload","title":"Reuse the same fetched payload","text":"<p>Once the first terminal method runs, later terminal methods use the cached payload rather than triggering a second fetch:</p> <pre><code>results = client.machines.get_all()\n\nrows = results.to_dicts()   # first fetch\ntable = results.to_arrow()  # reuses cached data\nframe = results.to_polars() # reuses cached data\n</code></pre> <p>This makes it practical to inspect the same result in multiple formats during one workflow.</p>"},{"location":"mde_client/how-to/work-with-result-formats/#refresh-the-cached-data","title":"Refresh the cached data","text":"<p>Materialization caches the underlying payload. Call <code>refresh()</code> before the next terminal method when you want a new API call:</p> <pre><code>fresh_rows = results.refresh().to_dicts()\n</code></pre> <p><code>refresh()</code> is chainable, so these patterns also work:</p> <pre><code>fresh_table = results.refresh().to_arrow()\nfresh_frame = results.refresh().to_polars()\n</code></pre>"},{"location":"mde_client/how-to/work-with-result-formats/#notes","title":"Notes","text":"<ul> <li><code>to_dicts()</code> is usually the best first choice for application code and debugging.</li> <li>Arrow and Polars support depend on your environment and package extras.</li> <li>The same materialization pattern is used across most endpoints.</li> <li>An empty response still materializes cleanly as <code>[]</code>, an empty table, or an empty dataframe.</li> </ul>"},{"location":"mde_client/how-to/work-with-result-formats/#next-references","title":"Next references","text":"<ul> <li>Results wrappers reference</li> <li>Lazy results and caching</li> </ul>"},{"location":"mde_client/reference/","title":"Reference","text":"<p>Reference pages describe the package surface as it exists today.</p>"},{"location":"mde_client/reference/#shared-reference","title":"Shared reference","text":"<ul> <li>MDEClient</li> <li>Authentication</li> <li>Results wrappers</li> <li>Query models</li> <li>ViaFiles</li> </ul>"},{"location":"mde_client/reference/#endpoint-reference","title":"Endpoint reference","text":"<ul> <li>Endpoint index</li> </ul> <p>Use the endpoint pages when you need the exact property name, method name, query model, payload model, or return wrapper.</p>"},{"location":"mde_client/reference/authentication/","title":"Authentication","text":"<p>The package uses MSAL client-credentials authentication.</p>"},{"location":"mde_client/reference/authentication/#what-the-client-does","title":"What the client does","text":"<ul> <li>builds an <code>msal.ConfidentialClientApplication</code></li> <li>uses the authority <code>https://login.microsoftonline.com/{tenant_id}</code></li> <li>requests the Defender default scope</li> <li>raises <code>AuthenticationError</code> when MSAL does not return an access token</li> </ul>"},{"location":"mde_client/reference/authentication/#inputs","title":"Inputs","text":"<p>Authentication is driven by these constructor values on <code>MDEClient</code>:</p> <ul> <li><code>tenant_id</code></li> <li><code>client_id</code></li> <li><code>client_secret</code></li> <li>optional <code>token_cache</code></li> </ul>"},{"location":"mde_client/reference/authentication/#token-cache-behavior","title":"Token cache behavior","text":"<p>If you do not pass a token cache, the client uses an in-memory <code>msal.TokenCache</code>. If you need persistence, inject your own cache implementation.</p>"},{"location":"mde_client/reference/authentication/#error-model","title":"Error model","text":"<p><code>AuthenticationError</code> is raised when:</p> <ul> <li>MSAL returns <code>None</code></li> <li>MSAL returns a result without <code>access_token</code></li> </ul> <p>HTTP-level API failures that happen after authentication are not wrapped as <code>AuthenticationError</code>; they bubble up through <code>httpx</code> status handling.</p>"},{"location":"mde_client/reference/authentication/#see-also","title":"See also","text":"<ul> <li>Authenticate with client credentials</li> <li>MDEClient</li> </ul>"},{"location":"mde_client/reference/mde-client/","title":"<code>MDEClient</code>","text":"<p><code>MDEClient</code> is the top-level entry point for the package.</p>"},{"location":"mde_client/reference/mde-client/#constructor","title":"Constructor","text":"<pre><code>MDEClient(\n    tenant_id: str,\n    client_id: str,\n    client_secret: str,\n    *,\n    base_url: str = \"https://api.securitycenter.microsoft.com\",\n    token_cache: msal.TokenCache | None = None,\n    http_client: httpx.Client | None = None,\n)\n</code></pre>"},{"location":"mde_client/reference/mde-client/#parameters","title":"Parameters","text":"<ul> <li><code>tenant_id</code>: Azure AD tenant ID.</li> <li><code>client_id</code>: app registration client ID.</li> <li><code>client_secret</code>: app registration client secret.</li> <li><code>base_url</code>: optional override for the Defender API base URL.</li> <li><code>token_cache</code>: optional MSAL token cache.</li> <li><code>http_client</code>: optional pre-configured <code>httpx.Client</code>.</li> </ul>"},{"location":"mde_client/reference/mde-client/#lifecycle","title":"Lifecycle","text":"<p><code>MDEClient</code> is a context manager:</p> <pre><code>with MDEClient(...) as client:\n    rows = client.machines.get_all().to_dicts()\n</code></pre> <p>If you do not use the context-manager form, call <code>close()</code> when you are done.</p>"},{"location":"mde_client/reference/mde-client/#top-level-endpoint-properties","title":"Top-level endpoint properties","text":"<ul> <li><code>advanced_queries</code></li> <li><code>alerts</code></li> <li><code>authenticated_definitions</code></li> <li><code>authenticated_agents</code></li> <li><code>browser_extension</code></li> <li><code>certificate_inventory</code></li> <li><code>device_av_health</code></li> <li><code>domain</code></li> <li><code>files</code></li> <li><code>indicators</code></li> <li><code>investigations</code></li> <li><code>ips</code></li> <li><code>library</code></li> <li><code>machine_actions</code></li> <li><code>machines</code></li> <li><code>recommendations</code></li> <li><code>remediations</code></li> <li><code>score</code></li> <li><code>baseline_configurations</code></li> <li><code>software</code></li> <li><code>user</code></li> <li><code>vulnerabilities</code></li> </ul> <p>See Endpoint index for endpoint-specific pages.</p>"},{"location":"mde_client/reference/mde-client/#public-top-level-imports","title":"Public top-level imports","text":"<p>The package exports these names from <code>mde_client</code>:</p> <ul> <li><code>MDEClient</code></li> <li><code>AuthenticationError</code></li> <li><code>ViaFiles</code></li> <li><code>ViaFilesConfig</code></li> <li><code>EmptyExportBlobError</code></li> </ul>"},{"location":"mde_client/reference/queries/","title":"Query Models","text":"<p>Query models inherit from <code>BaseQuery</code>.</p>"},{"location":"mde_client/reference/queries/#shared-fields","title":"Shared fields","text":"<p>Every query model can inherit these pagination controls:</p> <ul> <li><code>page_size</code> mapped to <code>pageSize</code></li> <li><code>top</code> mapped to <code>$top</code></li> <li><code>skip</code> mapped to <code>$skip</code></li> </ul>"},{"location":"mde_client/reference/queries/#filter-translation","title":"Filter translation","text":"<p>Non-null model fields are converted into OData-style filters.</p> <ul> <li>strings become <code>field eq 'value'</code></li> <li>datetimes become ISO-formatted equality filters</li> <li>lists of strings become <code>field in (...)</code></li> </ul> <p>Fields set to <code>None</code> are omitted.</p>"},{"location":"mde_client/reference/queries/#naming-convention","title":"Naming convention","text":"<p>Defender-specific query models preserve upstream API field names instead of normalizing them to snake_case. For example:</p> <ul> <li><code>healthStatus</code></li> <li><code>machineTags</code></li> <li><code>lastSeen</code></li> <li><code>creationDateTimeUtc</code></li> </ul> <p>This keeps query construction close to the upstream API surface and avoids a second naming layer in the package.</p>"},{"location":"mde_client/reference/queries/#common-endpoint-query-models","title":"Common endpoint query models","text":"<ul> <li><code>MachinesQuery</code></li> <li><code>AlertsQuery</code></li> <li><code>IndicatorsQuery</code></li> <li><code>InvestigationQuery</code></li> <li><code>MachineActionsQuery</code></li> <li><code>RecommendationQuery</code></li> <li><code>RemediationQuery</code></li> <li><code>SoftwareQuery</code></li> <li><code>VulnerabilitiesQuery</code></li> <li><code>VulnerabilitiesByMachineAndSoftwareQuery</code></li> </ul>"},{"location":"mde_client/reference/queries/#see-also","title":"See also","text":"<ul> <li>List and filter machines</li> <li>Why query models keep Defender field names</li> </ul>"},{"location":"mde_client/reference/results/","title":"Results Wrappers","text":"<p>Most endpoint methods return a lazy results wrapper instead of an eager list.</p>"},{"location":"mde_client/reference/results/#shared-interface","title":"Shared interface","text":"<p>The main wrapper type is <code>BaseResults</code>. It exposes:</p> <ul> <li><code>to_dicts()</code></li> <li><code>to_json(indent: bool = False)</code></li> <li><code>to_arrow()</code></li> <li><code>to_polars()</code></li> <li><code>refresh()</code></li> </ul> <p>The advanced hunting endpoint uses a dedicated wrapper, but it follows the same materialization interface.</p>"},{"location":"mde_client/reference/results/#fetch-behavior","title":"Fetch behavior","text":"<p>The wrapper does not fetch on construction. The first terminal method triggers the HTTP request and caches the result in memory.</p>"},{"location":"mde_client/reference/results/#pagination-behavior","title":"Pagination behavior","text":"<ul> <li>collection endpoints paginate automatically by default</li> <li>if a query sets <code>$top</code> or <code>$skip</code>, the wrapper performs only that request</li> <li>single-item methods use <code>single=True</code> and normalize the response to a one-record list when materialized as dictionaries</li> </ul>"},{"location":"mde_client/reference/results/#file-backed-behavior","title":"File-backed behavior","text":"<p>Some wrappers use <code>files=True</code>. In that case the initial response is expected to contain export URLs, and the wrapper downloads the export blobs through <code>ViaFiles</code> before returning the final materialized result.</p>"},{"location":"mde_client/reference/results/#materialization-choices","title":"Materialization choices","text":"<ul> <li><code>to_dicts()</code> is the simplest Python-native format</li> <li><code>to_json()</code> is useful for logging or serialization</li> <li><code>to_arrow()</code> returns a <code>pyarrow.Table</code></li> <li><code>to_polars()</code> returns a Polars DataFrame</li> <li><code>refresh()</code> clears the cache so the next terminal method fetches again</li> </ul>"},{"location":"mde_client/reference/results/#see-also","title":"See also","text":"<ul> <li>Work with result formats</li> <li>Lazy results and caching</li> </ul>"},{"location":"mde_client/reference/via-files/","title":"<code>ViaFiles</code>","text":"<p><code>ViaFiles</code> is the async downloader used for Defender file-based export workflows.</p>"},{"location":"mde_client/reference/via-files/#public-exports","title":"Public exports","text":"<ul> <li><code>ViaFiles</code></li> <li><code>ViaFilesConfig</code></li> <li><code>EmptyExportBlobError</code></li> </ul>"},{"location":"mde_client/reference/via-files/#what-viafiles-does","title":"What <code>ViaFiles</code> does","text":"<p>Given a set of export URLs, <code>ViaFiles</code>:</p> <ul> <li>downloads blobs concurrently</li> <li>detects gzip or plain text responses</li> <li>parses newline-delimited JSON incrementally</li> <li>batches records into an <code>ArrowRecordContainer</code></li> <li>retries transient failures</li> </ul> <p>Most package users do not need to call <code>ViaFiles</code> directly because export-backed endpoint wrappers already use it internally.</p>"},{"location":"mde_client/reference/via-files/#viafilesconfig","title":"<code>ViaFilesConfig</code>","text":"<p><code>ViaFilesConfig</code> exposes these tuning fields:</p> <ul> <li><code>download_workers</code></li> <li><code>client_timeout</code></li> <li><code>retry_attempts</code></li> <li><code>retry_delay_seconds</code></li> <li><code>download_chunk_size</code></li> <li><code>parse_batch_size</code></li> </ul> <p>These settings control concurrency, timeout behavior, retry policy, and streaming batch sizes.</p>"},{"location":"mde_client/reference/via-files/#error-behavior","title":"Error behavior","text":"<p><code>EmptyExportBlobError</code> is raised internally when a blob returns <code>200 OK</code> but contains no records. After retries, confirmed-empty blobs are skipped instead of aborting the whole download.</p> <p>Repeated hard failures raise a runtime error.</p>"},{"location":"mde_client/reference/via-files/#see-also","title":"See also","text":"<ul> <li>Use export-backed endpoints</li> <li>How export-backed endpoints work</li> </ul>"},{"location":"mde_client/reference/endpoints/","title":"Endpoint Reference","text":"<p>Each page in this section documents one public endpoint property exposed by <code>MDEClient</code>.</p> <ul> <li>advanced_queries</li> <li>alerts</li> <li>authenticated_definitions</li> <li>authenticated_agents</li> <li>browser_extension</li> <li>certificate_inventory</li> <li>device_av_health</li> <li>domain</li> <li>files</li> <li>indicators</li> <li>investigations</li> <li>ips</li> <li>library</li> <li>machine_actions</li> <li>machines</li> <li>recommendations</li> <li>remediations</li> <li>score</li> <li>baseline_configurations</li> <li>software</li> <li>user</li> <li>vulnerabilities</li> </ul> <p>These pages focus on the user-facing endpoint methods. Internal helper methods, such as the machine endpoint's private export helpers, are covered only when they affect the public behavior of another endpoint property.</p>"},{"location":"mde_client/reference/endpoints/advanced-queries/","title":"<code>client.advanced_queries</code>","text":"<p>Execute Defender advanced hunting queries.</p>"},{"location":"mde_client/reference/endpoints/advanced-queries/#property","title":"Property","text":"<p><code>client.advanced_queries</code></p>"},{"location":"mde_client/reference/endpoints/advanced-queries/#methods","title":"Methods","text":"<ul> <li><code>run(query: str) -&gt; AdvancedHuntingQueriesResults</code>: run a KQL-style advanced hunting query and return a lazy wrapper.</li> </ul>"},{"location":"mde_client/reference/endpoints/advanced-queries/#return-behavior","title":"Return behavior","text":"<p><code>AdvancedHuntingQueriesResults</code> exposes the same terminal methods used by the shared results wrappers:</p> <ul> <li><code>to_dicts()</code></li> <li><code>to_json()</code></li> <li><code>to_arrow()</code></li> <li><code>to_polars()</code></li> <li><code>refresh()</code></li> </ul>"},{"location":"mde_client/reference/endpoints/advanced-queries/#notes","title":"Notes","text":"<ul> <li>The output schema depends on the query you run.</li> <li>The result wrapper caches materialized data until <code>refresh()</code> is called.</li> </ul>"},{"location":"mde_client/reference/endpoints/alerts/","title":"<code>client.alerts</code>","text":"<p>Work with alerts and alert-related entities.</p>"},{"location":"mde_client/reference/endpoints/alerts/#property","title":"Property","text":"<p><code>client.alerts</code></p>"},{"location":"mde_client/reference/endpoints/alerts/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: AlertsQuery | None = None) -&gt; AlertsResults</code>: list alerts with optional filtering.</li> <li><code>get(id: str) -&gt; AlertsResults</code>: fetch one alert by ID.</li> <li><code>domains(id: str) -&gt; DomainResults</code>: fetch domains related to an alert.</li> <li><code>files(id: str) -&gt; FileResults</code>: fetch files related to an alert.</li> <li><code>ips(id: str) -&gt; IPResults</code>: fetch IPs related to an alert.</li> <li><code>machines(id: str) -&gt; MachineResults</code>: fetch machines related to an alert.</li> <li><code>user(id: str) -&gt; UserResults</code>: fetch the user related to an alert.</li> <li><code>createAlertByReference(payload) -&gt; AlertsResults</code>: create an alert by reference and return the created alert wrapper.</li> <li><code>batchUpdate(payload) -&gt; bool</code>: batch-update alerts and return success as a boolean.</li> <li><code>update(payload) -&gt; AlertsResults</code>: update a single alert and return the updated alert wrapper.</li> </ul>"},{"location":"mde_client/reference/endpoints/alerts/#notes","title":"Notes","text":"<ul> <li><code>get_all()</code> uses <code>AlertsQuery</code>, which inherits the shared pagination fields.</li> <li><code>batchUpdate()</code> is one of the endpoints that returns a success boolean instead of a results wrapper.</li> <li><code>update()</code> is separate from <code>batchUpdate()</code> and targets a single alert ID.</li> </ul>"},{"location":"mde_client/reference/endpoints/authenticated-agents/","title":"<code>client.authenticated_agents</code>","text":"<p>Inspect authenticated scan agents.</p>"},{"location":"mde_client/reference/endpoints/authenticated-agents/#property","title":"Property","text":"<p><code>client.authenticated_agents</code></p>"},{"location":"mde_client/reference/endpoints/authenticated-agents/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: DeviceAuthenticatedAgentsQuery | None = None) -&gt; DeviceAuthenticatedAgentsQueryResults</code>: list scan agents.</li> <li><code>get(id: str) -&gt; DeviceAuthenticatedAgentsQueryResults</code>: fetch one scan agent by ID.</li> </ul>"},{"location":"mde_client/reference/endpoints/authenticated-agents/#notes","title":"Notes","text":"<ul> <li><code>DeviceAuthenticatedAgentsQuery</code> is currently a placeholder query model and exists mainly for consistency and future-proofing.</li> <li>Both methods use the standard lazy materialization pattern.</li> </ul>"},{"location":"mde_client/reference/endpoints/authenticated-definitions/","title":"<code>client.authenticated_definitions</code>","text":"<p>Manage authenticated scan definitions and their history.</p>"},{"location":"mde_client/reference/endpoints/authenticated-definitions/#property","title":"Property","text":"<p><code>client.authenticated_definitions</code></p>"},{"location":"mde_client/reference/endpoints/authenticated-definitions/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: AuthenticatedDefinitionsQuery | None = None) -&gt; AuthenticatedDefinitionsResults</code>: list authenticated scan definitions.</li> <li><code>definition_history(ids: str | list[str], query: AuthenticatedScanHistoryQuery | None = None) -&gt; AuthenticatedScanHistoryResults</code>: fetch scan history by definition ID.</li> <li><code>session_history(ids: str | list[str], query: AuthenticatedScanHistoryQuery | None = None) -&gt; AuthenticatedScanHistoryResults</code>: fetch scan history by session ID.</li> <li><code>add(payload: AuthenticatedDefinitionsAlterPayload) -&gt; AuthenticatedDefinitionsResults</code>: create a scan definition.</li> <li><code>update(payload: AuthenticatedDefinitionsAlterPayload) -&gt; AuthenticatedDefinitionsResults</code>: update a scan definition.</li> <li><code>delete(ids: str | list[str]) -&gt; AuthenticatedDefinitionsResults</code>: delete one or more scan definitions.</li> </ul>"},{"location":"mde_client/reference/endpoints/authenticated-definitions/#notes","title":"Notes","text":"<ul> <li>The history endpoints use POST requests with explicit request bodies even though they read data.</li> <li>Mutating methods still return lazy results wrappers rather than booleans.</li> <li><code>AuthenticatedScanHistoryQuery</code> disables the inherited <code>page_size</code> default so it does not leak into POST bodies.</li> </ul>"},{"location":"mde_client/reference/endpoints/baseline-configurations/","title":"<code>client.baseline_configurations</code>","text":"<p>Access security baseline assessments, profiles, and active configurations.</p>"},{"location":"mde_client/reference/endpoints/baseline-configurations/#property","title":"Property","text":"<p><code>client.baseline_configurations</code></p>"},{"location":"mde_client/reference/endpoints/baseline-configurations/#methods","title":"Methods","text":"<ul> <li><code>get_all() -&gt; AssetBaselineAssessmentResults</code>: fetch per-device baseline assessment rows.</li> <li><code>get_all_files() -&gt; AssetBaselineAssessmentResults</code>: fetch per-device baseline assessment rows through the export-backed path.</li> <li><code>profiles() -&gt; BaselineConfigurationResults</code>: list baseline profiles created by the organization.</li> <li><code>active() -&gt; BaselineConfigurationResults</code>: list configurations being assessed in active baseline profiles.</li> </ul>"},{"location":"mde_client/reference/endpoints/baseline-configurations/#notes","title":"Notes","text":"<ul> <li><code>get_all()</code> and <code>get_all_files()</code> return assessment rows, not the same wrapper used by <code>profiles()</code> and <code>active()</code>.</li> <li>Use <code>profiles()</code> and <code>active()</code> when you need baseline metadata rather than per-device assessment output.</li> </ul>"},{"location":"mde_client/reference/endpoints/browser-extension/","title":"<code>client.browser_extension</code>","text":"<p>Access browser extension inventory and permission metadata.</p>"},{"location":"mde_client/reference/endpoints/browser-extension/#property","title":"Property","text":"<p><code>client.browser_extension</code></p>"},{"location":"mde_client/reference/endpoints/browser-extension/#methods","title":"Methods","text":"<ul> <li><code>get_all() -&gt; BrowserExtensionResults</code>: fetch the JSON-backed browser extension inventory.</li> <li><code>get_all_files() -&gt; BrowserExtensionResults</code>: fetch the via-files browser extension inventory.</li> <li><code>permissionsinfo(query: BrowserExtensionsPermissionsInfoQuery | None = None) -&gt; BrowserExtensionsPermissionsInfoResults</code>: fetch permission metadata for browser extensions.</li> </ul>"},{"location":"mde_client/reference/endpoints/browser-extension/#notes","title":"Notes","text":"<ul> <li><code>get_all()</code> and <code>get_all_files()</code> expose the same results-wrapper interface.</li> <li><code>get_all_files()</code> delegates to the machine endpoint's export-backed implementation.</li> <li>Use <code>permissionsinfo()</code> when you need extension permission metadata rather than per-machine inventory rows.</li> </ul>"},{"location":"mde_client/reference/endpoints/certificate-inventory/","title":"<code>client.certificate_inventory</code>","text":"<p>Access certificate inventory assessment exports.</p>"},{"location":"mde_client/reference/endpoints/certificate-inventory/#property","title":"Property","text":"<p><code>client.certificate_inventory</code></p>"},{"location":"mde_client/reference/endpoints/certificate-inventory/#methods","title":"Methods","text":"<ul> <li><code>get_all() -&gt; CertificateInventoryResults</code>: fetch the JSON-backed certificate inventory assessment.</li> <li><code>get_all_files() -&gt; CertificateInventoryResults</code>: fetch the via-files certificate inventory assessment.</li> </ul>"},{"location":"mde_client/reference/endpoints/certificate-inventory/#notes","title":"Notes","text":"<ul> <li>Both methods produce the same results-wrapper type.</li> <li>Choose <code>get_all_files()</code> when you want the export-backed workflow.</li> </ul>"},{"location":"mde_client/reference/endpoints/device-av-health/","title":"<code>client.device_av_health</code>","text":"<p>Access device antivirus health reporting.</p>"},{"location":"mde_client/reference/endpoints/device-av-health/#property","title":"Property","text":"<p><code>client.device_av_health</code></p>"},{"location":"mde_client/reference/endpoints/device-av-health/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: DeviceAVHealthQuery | None = None) -&gt; DeviceAVHealthResults</code>: fetch the JSON-backed AV health report.</li> <li><code>get_all_files() -&gt; DeviceAVHealthResults</code>: fetch the via-files AV health report.</li> </ul>"},{"location":"mde_client/reference/endpoints/device-av-health/#notes","title":"Notes","text":"<ul> <li>The export-backed path normalizes nested blob records into the same schema used by the JSON response.</li> <li><code>DeviceAVHealthQuery</code> keeps Defender field names such as <code>machineId</code>, <code>computerDnsName</code>, and <code>avSignatureVersion</code>.</li> </ul>"},{"location":"mde_client/reference/endpoints/domain/","title":"<code>client.domain</code>","text":"<p>Look up domain relationships and statistics.</p>"},{"location":"mde_client/reference/endpoints/domain/#property","title":"Property","text":"<p><code>client.domain</code></p>"},{"location":"mde_client/reference/endpoints/domain/#methods","title":"Methods","text":"<ul> <li><code>alerts(domain: str) -&gt; AlertsResults</code>: fetch alerts related to a domain.</li> <li><code>machines(domain: str) -&gt; MachineResults</code>: fetch machines related to a domain.</li> <li><code>stats(domain: str) -&gt; DomainStatsResults</code>: fetch in-organization statistics for a domain.</li> </ul>"},{"location":"mde_client/reference/endpoints/domain/#notes","title":"Notes","text":"<ul> <li>This endpoint is relationship-oriented. It does not expose a domain collection listing method.</li> </ul>"},{"location":"mde_client/reference/endpoints/files/","title":"<code>client.files</code>","text":"<p>Look up file details and related entities.</p>"},{"location":"mde_client/reference/endpoints/files/#property","title":"Property","text":"<p><code>client.files</code></p>"},{"location":"mde_client/reference/endpoints/files/#methods","title":"Methods","text":"<ul> <li><code>get(hash: str) -&gt; FileResults</code>: fetch file metadata by hash.</li> <li><code>alerts(hash: str) -&gt; AlertsResults</code>: fetch alerts related to a file.</li> <li><code>machines(hash: str) -&gt; MachineResults</code>: fetch machines related to a file.</li> <li><code>stats(hash: str) -&gt; FileStatsResults</code>: fetch in-organization file statistics.</li> </ul>"},{"location":"mde_client/reference/endpoints/files/#notes","title":"Notes","text":"<ul> <li>This endpoint uses the file hash as the lookup key.</li> <li>Relationship methods return the same lazy wrappers used by their primary endpoint families.</li> </ul>"},{"location":"mde_client/reference/endpoints/indicators/","title":"<code>client.indicators</code>","text":"<p>Manage threat indicators.</p>"},{"location":"mde_client/reference/endpoints/indicators/#property","title":"Property","text":"<p><code>client.indicators</code></p>"},{"location":"mde_client/reference/endpoints/indicators/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: IndicatorsQuery | None = None) -&gt; IndicatorsResults</code>: list indicators with optional filters.</li> <li><code>submit(payload: IndicatorsSubmitPayload) -&gt; IndicatorsResults</code>: create a single indicator.</li> <li><code>batch_import(payload: list[IndicatorsSubmitPayload]) -&gt; ImportIndicatorResults | list[ImportIndicatorResults]</code>: import indicators in batches.</li> <li><code>delete(id: str) -&gt; bool</code>: delete one indicator by ID.</li> <li><code>batch_delete(ids: list[str]) -&gt; bool</code>: delete multiple indicators by ID.</li> </ul>"},{"location":"mde_client/reference/endpoints/indicators/#notes","title":"Notes","text":"<ul> <li><code>batch_import()</code> chunks requests larger than 500 indicators and can return a list of result wrappers.</li> <li><code>delete()</code> and <code>batch_delete()</code> return booleans instead of results wrappers.</li> <li><code>batch_delete()</code> also chunks oversized requests.</li> </ul>"},{"location":"mde_client/reference/endpoints/investigations/","title":"<code>client.investigations</code>","text":"<p>Inspect and start investigations.</p>"},{"location":"mde_client/reference/endpoints/investigations/#property","title":"Property","text":"<p><code>client.investigations</code></p>"},{"location":"mde_client/reference/endpoints/investigations/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: InvestigationQuery | None = None) -&gt; InvestigationResults</code>: list investigations.</li> <li><code>get(id: str) -&gt; InvestigationResults</code>: fetch one investigation by ID.</li> <li><code>startInvestigation(deviceId: str, payload: StartInvestigationPayload) -&gt; InvestigationResults</code>: start an investigation for a device.</li> </ul>"},{"location":"mde_client/reference/endpoints/investigations/#notes","title":"Notes","text":"<ul> <li><code>startInvestigation()</code> is exposed from the investigations property but delegates to a machine-scoped action under the hood.</li> <li>Read methods use the standard lazy wrapper behavior.</li> </ul>"},{"location":"mde_client/reference/endpoints/ips/","title":"<code>client.ips</code>","text":"<p>Look up IP relationships and statistics.</p>"},{"location":"mde_client/reference/endpoints/ips/#property","title":"Property","text":"<p><code>client.ips</code></p>"},{"location":"mde_client/reference/endpoints/ips/#methods","title":"Methods","text":"<ul> <li><code>alerts(ip: str) -&gt; AlertsResults</code>: fetch alerts related to an IP address.</li> <li><code>stats(ip: str) -&gt; InOrgIPStatsResults</code>: fetch in-organization statistics for an IP address.</li> </ul>"},{"location":"mde_client/reference/endpoints/ips/#notes","title":"Notes","text":"<ul> <li>This endpoint is relationship-oriented. It does not expose a general IP collection listing method.</li> </ul>"},{"location":"mde_client/reference/endpoints/library/","title":"<code>client.library</code>","text":"<p>Manage live response library files.</p>"},{"location":"mde_client/reference/endpoints/library/#property","title":"Property","text":"<p><code>client.library</code></p>"},{"location":"mde_client/reference/endpoints/library/#methods","title":"Methods","text":"<ul> <li><code>get_all() -&gt; LibraryFilesResults</code>: list live response library files.</li> <li><code>upload(payload: LibraryFilesUpdatePayload) -&gt; bool</code>: upload a library file.</li> <li><code>delete(file_name: str) -&gt; bool</code>: delete a library file.</li> </ul>"},{"location":"mde_client/reference/endpoints/library/#notes","title":"Notes","text":"<ul> <li><code>upload()</code> and <code>delete()</code> return booleans rather than results wrappers.</li> <li><code>LibraryFilesUpdatePayload</code> includes file metadata such as <code>file_name</code>, <code>file_content</code>, <code>content_type</code>, and descriptions.</li> </ul>"},{"location":"mde_client/reference/endpoints/machine-actions/","title":"<code>client.machine_actions</code>","text":"<p>Inspect machine actions and trigger machine response operations.</p>"},{"location":"mde_client/reference/endpoints/machine-actions/#property","title":"Property","text":"<p><code>client.machine_actions</code></p>"},{"location":"mde_client/reference/endpoints/machine-actions/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: MachineActionsQuery | None = None) -&gt; MachineActionsResults</code>: list machine actions.</li> <li><code>get(id: str) -&gt; MachineActionsResults</code>: fetch one machine action by ID.</li> <li><code>collectInvestigationPackage(device_id: str, payload) -&gt; MachineActionsResults</code>: collect an investigation package.</li> <li><code>getPackage(id: str) -&gt; str</code>: return a package download URI for a completed investigation package action.</li> <li><code>getLiveResponseResultDownloadLink(id: str, index: int = 0) -&gt; str</code>: return a live response result download URI.</li> <li><code>isolate(device_id: str, payload) -&gt; MachineActionsResults</code>: isolate a machine.</li> <li><code>unisolate(device_id: str, payload) -&gt; MachineActionsResults</code>: remove isolation.</li> <li><code>restrictCodeExecution(device_id: str, payload) -&gt; MachineActionsResults</code>: restrict code execution.</li> <li><code>unrestrictCodeExecution(device_id: str, payload) -&gt; MachineActionsResults</code>: remove code execution restrictions.</li> <li><code>runAntiVirusScan(device_id: str, payload) -&gt; MachineActionsResults</code>: trigger an antivirus scan.</li> <li><code>runLiveResponse(device_id: str, payload) -&gt; MachineActionsResults</code>: run a live response command.</li> <li><code>offBoard(device_id: str, payload) -&gt; MachineActionsResults</code>: off-board a machine.</li> <li><code>stopAndQuarantineFile(device_id: str, payload) -&gt; MachineActionsResults</code>: stop and quarantine a file.</li> <li><code>cancel(id: str, payload) -&gt; MachineActionsResults</code>: cancel a machine action.</li> </ul>"},{"location":"mde_client/reference/endpoints/machine-actions/#notes","title":"Notes","text":"<ul> <li>Most mutating methods return a <code>MachineActionsResults</code> wrapper rather than a boolean.</li> <li><code>getPackage()</code> and <code>getLiveResponseResultDownloadLink()</code> are the exceptions: they return temporary download URLs as strings.</li> <li>The action methods delegate to machine-scoped paths under the hood, but the public entry point is <code>client.machine_actions</code>.</li> </ul>"},{"location":"mde_client/reference/endpoints/machines/","title":"<code>client.machines</code>","text":"<p>Access machine inventory and machine-scoped related resources.</p>"},{"location":"mde_client/reference/endpoints/machines/#property","title":"Property","text":"<p><code>client.machines</code></p>"},{"location":"mde_client/reference/endpoints/machines/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: MachinesQuery | None = None) -&gt; MachineResults</code>: list machines with optional filtering.</li> <li><code>get(id: str) -&gt; MachineResults</code>: fetch one machine by ID.</li> <li><code>logonusers(id: str) -&gt; UserResults</code>: fetch machine logon users.</li> <li><code>alerts(id: str) -&gt; AlertsResults</code>: fetch alerts related to a machine.</li> <li><code>software(id: str) -&gt; SoftwareResults</code>: fetch installed software for a machine.</li> <li><code>vulnerabilities(id: str) -&gt; VulnerabilityDTOResults</code>: fetch vulnerabilities for a machine.</li> <li><code>recommendations(id: str) -&gt; RecommendationResults</code>: fetch recommendations for a machine.</li> <li><code>getmissingkbs(id: str) -&gt; ProductDTOResults</code>: fetch missing KBs for a machine.</li> <li><code>findbyip(ip: str, timestamp: datetime) -&gt; MachineResults</code>: find machines by internal IP and timestamp.</li> <li><code>tag(tag: str, useStartsWithFilter: bool = False) -&gt; MachineResults</code>: find machines by tag.</li> </ul>"},{"location":"mde_client/reference/endpoints/machines/#notes","title":"Notes","text":"<ul> <li><code>get_all()</code> auto-paginates unless <code>top</code> or <code>skip</code> is set in <code>MachinesQuery</code>.</li> <li><code>findbyip()</code> normalizes timestamps to UTC before constructing the request path.</li> <li>This endpoint also contains private helper methods that power public export-backed endpoints such as <code>browser_extension</code>, <code>certificate_inventory</code>, <code>device_av_health</code>, <code>investigations</code>, <code>machine_actions</code>, and <code>baseline_configurations</code>.</li> </ul>"},{"location":"mde_client/reference/endpoints/recommendations/","title":"<code>client.recommendations</code>","text":"<p>Access recommendation records and recommendation-related entities.</p>"},{"location":"mde_client/reference/endpoints/recommendations/#property","title":"Property","text":"<p><code>client.recommendations</code></p>"},{"location":"mde_client/reference/endpoints/recommendations/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: RecommendationQuery | None = None) -&gt; RecommendationResults</code>: list recommendations.</li> <li><code>get(id: str) -&gt; RecommendationResults</code>: fetch one recommendation by ID.</li> <li><code>software(id: str) -&gt; ProductDTOResults</code>: fetch software related to a recommendation.</li> <li><code>machineReferences(id: str) -&gt; MachineReferencesResults</code>: fetch machine references for a recommendation.</li> <li><code>vulnerabilities(id: str) -&gt; VulnerabilityDTOResults</code>: fetch vulnerabilities related to a recommendation.</li> </ul>"},{"location":"mde_client/reference/endpoints/recommendations/#notes","title":"Notes","text":"<ul> <li>The method name <code>machineReferences</code> uses camelCase because it follows the current package surface.</li> </ul>"},{"location":"mde_client/reference/endpoints/remediations/","title":"<code>client.remediations</code>","text":"<p>Access remediation tasks and their machine references.</p>"},{"location":"mde_client/reference/endpoints/remediations/#property","title":"Property","text":"<p><code>client.remediations</code></p>"},{"location":"mde_client/reference/endpoints/remediations/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: RemediationQuery | None = None) -&gt; RemediationResults</code>: list remediation tasks.</li> <li><code>get(id: str) -&gt; RemediationResults</code>: fetch one remediation task by ID.</li> <li><code>machinereferences(id: str) -&gt; MachineReferencesResults</code>: fetch machine references for a remediation task.</li> </ul>"},{"location":"mde_client/reference/endpoints/remediations/#notes","title":"Notes","text":"<ul> <li>The relationship method is currently named <code>machinereferences</code> in lowercase.</li> </ul>"},{"location":"mde_client/reference/endpoints/score/","title":"<code>client.score</code>","text":"<p>Access exposure and configuration score data.</p>"},{"location":"mde_client/reference/endpoints/score/#property","title":"Property","text":"<p><code>client.score</code></p>"},{"location":"mde_client/reference/endpoints/score/#methods","title":"Methods","text":"<ul> <li><code>get() -&gt; ScoreResults</code>: fetch the organization exposure score.</li> <li><code>byMachineGroups() -&gt; ScoreResults</code>: fetch exposure score by machine group.</li> <li><code>configurationScore() -&gt; ScoreResults</code>: fetch Microsoft Secure Score for Devices.</li> </ul>"},{"location":"mde_client/reference/endpoints/score/#notes","title":"Notes","text":"<ul> <li>All methods return the same <code>ScoreResults</code> wrapper even though they target different score paths.</li> </ul>"},{"location":"mde_client/reference/endpoints/software/","title":"<code>client.software</code>","text":"<p>Access software inventory and related records.</p>"},{"location":"mde_client/reference/endpoints/software/#property","title":"Property","text":"<p><code>client.software</code></p>"},{"location":"mde_client/reference/endpoints/software/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: SoftwareQuery | None = None) -&gt; SoftwareResults</code>: list software inventory records.</li> <li><code>get(id: str) -&gt; SoftwareResults</code>: fetch one software inventory record by ID.</li> <li><code>distributions(id: str) -&gt; DistributionDTOResults</code>: fetch software distribution records.</li> <li><code>machineReferences(id: str) -&gt; MachineReferencesResults</code>: fetch machines related to the software record.</li> <li><code>vulnerabilities(id: str) -&gt; VulnerabilityDTOResults</code>: fetch vulnerabilities related to the software record.</li> <li><code>getmissingkbs(id: str) -&gt; ProductDTOResults</code>: fetch missing KBs related to the software record.</li> </ul>"},{"location":"mde_client/reference/endpoints/software/#notes","title":"Notes","text":"<ul> <li>This endpoint uses <code>/api/Software</code> with an uppercase <code>S</code> because that is the current package path.</li> <li>The method name <code>machineReferences</code> follows the current mixed-case package surface.</li> </ul>"},{"location":"mde_client/reference/endpoints/user/","title":"<code>client.user</code>","text":"<p>Access user-related alert and machine relationships.</p>"},{"location":"mde_client/reference/endpoints/user/#property","title":"Property","text":"<p><code>client.user</code></p>"},{"location":"mde_client/reference/endpoints/user/#methods","title":"Methods","text":"<ul> <li><code>alerts(id: str) -&gt; AlertsResults</code>: fetch alerts related to a user.</li> <li><code>machines(id: str) -&gt; MachineResults</code>: fetch machines related to a user.</li> </ul>"},{"location":"mde_client/reference/endpoints/user/#notes","title":"Notes","text":"<ul> <li>The property name is <code>user</code>, not <code>users</code>.</li> <li>This endpoint is relationship-oriented and does not expose a user listing method.</li> </ul>"},{"location":"mde_client/reference/endpoints/vulnerabilities/","title":"<code>client.vulnerabilities</code>","text":"<p>Access vulnerability records and related machine views.</p>"},{"location":"mde_client/reference/endpoints/vulnerabilities/#property","title":"Property","text":"<p><code>client.vulnerabilities</code></p>"},{"location":"mde_client/reference/endpoints/vulnerabilities/#methods","title":"Methods","text":"<ul> <li><code>get_all(query: VulnerabilitiesQuery | None = None) -&gt; VulnerabilityResults</code>: list vulnerabilities.</li> <li><code>get(id: str) -&gt; VulnerabilityResults</code>: fetch one vulnerability by ID.</li> <li><code>machineReferences(id: str) -&gt; MachineResults</code>: fetch machines related to a vulnerability.</li> <li><code>machinesVulnerabilities(id: str) -&gt; VulnerabilitiesByMachineAndSoftwareResults</code>: fetch the machine-vulnerability collection path.</li> </ul>"},{"location":"mde_client/reference/endpoints/vulnerabilities/#notes","title":"Notes","text":"<ul> <li><code>machinesVulnerabilities()</code> currently accepts an <code>id</code> parameter but calls the collection path <code>/api/vulnerabilities/machinesVulnerabilities</code> rather than a per-ID path.</li> <li>Use the query models in the endpoint module when you need collection-level filtering for vulnerability datasets.</li> </ul>"},{"location":"mde_client/tutorials/get-started/","title":"Get Started","text":"<p>This tutorial helps you complete one successful workflow with <code>mde-client</code>: authenticate to Microsoft Defender for Endpoint, fetch a filtered machines result, and inspect the returned records.</p> <p>By the end, you will have a working <code>MDEClient</code> in your own code and a list of machine dictionaries you can build on.</p>"},{"location":"mde_client/tutorials/get-started/#before-you-begin","title":"Before you begin","text":"<p>You need:</p> <ul> <li>Python 3.14 or newer</li> <li>an Azure AD app registration that can use the client-credentials flow</li> <li>a tenant ID, client ID, and client secret</li> <li>Defender application permissions granted to that app registration</li> </ul>"},{"location":"mde_client/tutorials/get-started/#install-the-package","title":"Install the package","text":"<p>Install from a project that uses <code>uv</code>:</p> <pre><code>uv add mde-client\n</code></pre> <p>If you are working inside this repository instead of consuming the published package, use the workspace bootstrap flow:</p> <pre><code>uv sync --all-packages --all-groups\n</code></pre>"},{"location":"mde_client/tutorials/get-started/#create-a-client","title":"Create a client","text":"<p>Create a new file and start with a minimal client setup:</p> <pre><code>from mde_client import AuthenticationError, MDEClient\nfrom mde_client.endpoints.machines import MachinesQuery\n\ntry:\n    with MDEClient(\n        tenant_id=\"YOUR_TENANT_ID\",\n        client_id=\"YOUR_CLIENT_ID\",\n        client_secret=\"YOUR_CLIENT_SECRET\",\n    ) as client:\n        query = MachinesQuery(healthStatus=\"Active\", page_size=100)\n        results = client.machines.get_all(query)\nexcept AuthenticationError as exc:\n    raise SystemExit(f\"Authentication failed: {exc}\")\n</code></pre> <p>At this point no API request has been made yet. <code>results</code> is a lazy wrapper.</p>"},{"location":"mde_client/tutorials/get-started/#materialize-the-results","title":"Materialize the results","text":"<p>Trigger the request by calling a terminal method:</p> <pre><code>from mde_client import AuthenticationError, MDEClient\nfrom mde_client.endpoints.machines import MachinesQuery\n\ntry:\n    with MDEClient(\n        tenant_id=\"YOUR_TENANT_ID\",\n        client_id=\"YOUR_CLIENT_ID\",\n        client_secret=\"YOUR_CLIENT_SECRET\",\n    ) as client:\n        query = MachinesQuery(healthStatus=\"Active\", page_size=100)\n        machines = client.machines.get_all(query).to_dicts()\n\n        print(f\"Fetched {len(machines)} machines\")\n        if machines:\n            print(machines[0][\"computerDnsName\"])\nexcept AuthenticationError as exc:\n    raise SystemExit(f\"Authentication failed: {exc}\")\n</code></pre> <p><code>to_dicts()</code> returns a Python <code>list[dict]</code>, which is the simplest representation for a first integration.</p>"},{"location":"mde_client/tutorials/get-started/#read-one-machine-by-id","title":"Read one machine by ID","text":"<p>Once you have a machine ID, fetch a single machine record:</p> <pre><code>machine = client.machines.get(\"machine-guid\").to_dicts()[0]\nprint(machine[\"computerDnsName\"])\n</code></pre> <p>Single-item lookups still use the same results-wrapper pattern.</p>"},{"location":"mde_client/tutorials/get-started/#what-to-expect","title":"What to expect","text":"<p>When the tutorial succeeds:</p> <ul> <li>authentication completes without raising <code>AuthenticationError</code></li> <li><code>client.machines.get_all(...)</code> returns one or more records, or an empty list if the filter matches nothing</li> <li><code>to_dicts()</code> gives you ordinary Python dictionaries ready for downstream processing</li> </ul>"},{"location":"mde_client/tutorials/get-started/#next-steps","title":"Next steps","text":"<ul> <li>Learn common workflows in How-to guides</li> <li>Look up the full client surface in MDEClient reference</li> <li>Understand lazy execution in Lazy results and caching</li> </ul>"}]}